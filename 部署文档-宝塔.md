# 部署文档 · 宝塔面板

> 适用：综测计算工作台 v1.3。单机部署，只跑一个 Python 进程，前端构建产物由该进程静态托管，Nginx 仅做反向代理。

## 1. 环境准备

1. 宝塔面板安装「Python 项目管理器」（建议 Python 3.10+，本实践基于 3.14）；
2. 上传项目目录（含 `backend/` 与已构建的 `frontend/app/dist/`）至如 `/www/wwwroot/zongce`；
3. 无需安装 Node——前端在开发机 `npm run build` 后上传 `dist` 即可。

## 2. 后端安装与启动

```bash
cd /www/wwwroot/zongce/backend
python -m venv venv && source venv/bin/activate
pip install -r requirements.txt
```

Python 项目管理器中新建项目：

| 项 | 值 |
|---|---|
| 项目路径 | `/www/wwwroot/zongce/backend` |
| 启动文件/方式 | `uvicorn app.main:app --host 127.0.0.1 --port 8300` |
| Python 版本 | 项目 venv |

首次启动自动创建 `data/zongce.db` 并写入默认管理员 `admin / admin123`（**登录后立即改密**）。SQLite 已启用 WAL 与 `busy_timeout=5000`，可直接备份单文件（建议面板计划任务每日拷贝 `data/`）。

## 3. Nginx 反代

站点「设置 → 反向代理」目标 `http://127.0.0.1:8300`，配置文件确保：

```nginx
client_max_body_size 50m;          # 成绩 Excel 上传
proxy_set_header Host $host;
proxy_set_header X-Real-IP $remote_addr;
```

## 4. 验证清单

1. 浏览器打开站点 → 登录页正常，admin 首登强制改密；
2. 上传样本 `1.xls` → 预览学年/学期/学生数正确、异常清单可导出 → 确认入库；
3. 上传样本 `2.xlsx` → 班级模糊匹配确认 → 入库；
4. 导出中心选学年+年级 → 下载工作簿与 `output.xlsx` 对应 sheet 核对（学生/课程/加权平均值）；
5. 新建辅导员账号 → 首登强制改密 → 仅可见所辖年级。

## 5. 常见问题

- **502**：Python 项目未启动或端口不符，看面板项目日志；
- **上传 413**：Nginx `client_max_body_size` 未调大；
- **锁库 database is locked**：确认只有 一个 Python 进程 挂载数据库（WAL 已开，勿多进程写）；
- **忘记 admin 密码**：停服后用 `python -m app.reset_admin` 重置为 `admin123` 并重新上线。
