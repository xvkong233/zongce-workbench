# 综测计算工作台 · 产品设计文档

> 版本：v1.2（2026-09-02 前端选型升级为 Ant Design Pro 组件体系 + Ant Design 6）
> 形态：Web 平台（B/S），宝塔面板部署；前端 React 18 + Ant Design 6 + Ant Design Pro 组件体系（Vite 构建），后端 Python FastAPI + SQLite。

## 1. 背景与目标

高校学生综合素质测评（综测）目前依赖手工 Excel：教务导出的成绩长表（1.xls）需人工转成「学生 × 课程」二维宽表（output.xlsx），班级综测明细（2.xlsx）再人工合成总评。本平台将这些环节线上化：

1. 上传教务成绩表（1.xls/.xlsx）→ 自动识别学年/学期 → 一维长表转二维宽表；
2. 在线录入或导入班级综测明细（2.xlsx）；
3. 一键计算并导出 output.xlsx 风格的学年综测汇总工作簿。

## 2. 角色与权限

| 角色 | 能力 |
|---|---|
| 管理员 | 辅导员账号管理（新建/禁用/重置密码/删除、分配年级）；全局基础数据；综测方案与等级换算表配置；全部数据查看/操作；全局导出；操作日志 |
| 辅导员 | 绑定一个或多个**年级**；可新建学年/年级/班级（独立操作或导入时弹窗新建，新建年级自动与自己绑定）；仅能访问所辖年级：成绩导入、学生修正、综测录入、导出 |
| 学生 | 不登录，仅作为数据记录；导出中「确认签字」列留空供线下签字 |

系统预置管理员账号 `admin`（首次登录后请修改密码）。

## 3. 基础数据模型

```
学年 AcademicYear        名称如「2024-2025」，含秋季/春季学期
年级 Grade               名称如「24级」，入学年份 2024
班级 ClassInfo           名称如「建筑类2401」，隶属年级
学生 Student             学号（唯一）、姓名、所属班级；随成绩导入自动建档/更新，支持手动修正
辅导员-年级              多对多绑定
综测方案（按学年）        五个项目的名称/满分/基础分模板 + 合成权重
等级换算表（全局）        等级文本 → 百分制分数
```

- **综测方案默认值**：思想品德 25 / 社会工作 20 / 科研及科技创新 20 / 文体活动 15 / 集体建设 20（满分可改）；基础分模板：思想品德「基础分+23」、集体建设「班级基础分+7 / 寝室基础分+8」。
- **等级换算表默认值**（需求方提供）：百分制 0–100 直用；两级制：合格=80、不合格=0；五级制：优秀(优)=95、良好(良)=85、中等(中)=75、及格=65、不及格=0。**「合格」与「及格」严格区分**；未匹配等级记为异常，不入库。

## 4. 核心业务流程

### 4.1 成绩导入（1.xls / .xlsx）
1. 上传文件；系统读取全部 sheet，定位表头行（含「学号」「课程代码」），按「学年度/学期」列自动归类；
2. 字段类型归一：学号数字/文本统一为文本；学分/绩点文本数字转数值；分数兼容数字、文本数字与等级文本（等级按换算表转百分制，原始值留档）；
3. 学年/年级/班级不存在 → **弹窗提示新建**（年级从班级名数字推断，如「建筑类2401」→ 24级；辅导员新建年级自动绑定自己）；
4. 预览页展示：识别到的学年/学期、学生数、课程数、待新建项、异常清单（缺学号/课程代码、未知等级、缺学分/绩点、学生班级归属冲突）；
5. 确认后入库：按（学号+学年+学期+课程代码）合并去重，**重复导入以最新文件为准**（覆盖旧记录）；学生档案同步新建/更新。

### 4.2 综测录入（2.xlsx 线上化）
- **在线填写**：选学年+班级 → 名册逐人录入五个项目的「加减分明细文本 + 得分」，实时显示各项小计（按满分封顶）与五项合计；
- **Excel 导入（v1.3.3 起按年级）**：选择目标学年 + 目标年级 → 上传 2.xlsx，自动识别双层表头（项目大类 + 加减分项/得分）与合并单元格，**仅按学号匹配所选年级内的学生**（文件班级列仅作参考、无需映射）；未匹配者（年级外学号/不存在）不入库并在预览中标出；预览确认后入库（覆盖该批学生同年学年的旧记录）。

### 4.3 计算与导出（output.xlsx 风格）
导出向导：选学年 + 年级（可多选）→ 生成一个工作簿，每个年级两个 sheet：`XX级（成绩）`、`XX级（绩点）`。

- 成绩 sheet 表头 3 行：课程代码 / 课程名称 / 学分；绩点 sheet 表头 4 行：课程代码 / 课程名称 / 授课教师 / 学分（统一规范化，修正手工样本的不一致）；
- 标签行「班级 | 学号 | 姓名」；数据行按班级分组、学号升序；学生未修课程留空（稀疏矩阵）；
- 课程列排序：秋季课程在前、春季在后，同学期按课程代码；课程归属（代码→名称/学分/教师）取导入数据中出现次数最多者；
- 成绩矩阵填**换算后的百分制数字**（原始等级在学生成绩查询中留档）；绩点矩阵填绩点值；
- 尾部列：`加权总分 | 总学分 | 加权平均值 | 德育 | 思想品德 | 社会工作 | 科研及科技创新 | 文体活动 | 集体建设 | 综合素质测评 | 综合测评成绩 | 智育排名 | 综测排名 | 确认签字`；
  - 加权平均值/平均绩点单元格写入公式 `=加权总分/总学分` 便于核对；
  - 德育、确认签字留空；
- 导出范围：全校（多年级合一簿）/ 单年级 / 单班级（取该班学生所在年级的两个 sheet，仅含该班学生）。

## 5. 计算规则（已确认）

1. **等级→百分制**：见 §3 换算表；
2. **学业加权平均** = Σ(成绩 × 学分) / Σ(有成绩课程的学分)；缺学分/缺成绩的课程不计入；
3. **综测各项小计** = 明细得分，超过该项满分按满分封顶；
4. **综合素质测评** = 五项小计之和（默认满分 100）；
5. **综合测评成绩 = 学业加权平均 × 80% + 综合素质测评 × 20%**（权重按学年配置，默认 80/20）；
6. **排名（v1.3.2 起按专业，不再年级统排）**：专业组 = 同年级内、按班级「专业」字段分组；未填写专业字段的班级按班名自动提取（去掉尾部「班号数字+可选班字」，建筑类2401/建筑类2402班 → 建筑类、计科23班 → 计科；不含班号的班名如 试验班 保持原样）；**智育排名**在专业组内按学业加权平均降序、**综测排名**在专业组内按综合测评成绩降序；**同分同名次**（1,1,3 式）。排名口径与展示筛选无关：汇总页班级筛选、单班级导出、单个学生报告均按专业组全体计算；
7. 无综测记录的学生综合素质测评按 0 分参与计算，并在导出中可见。

## 6. 页面清单

| 页面 | 说明 | 角色 |
|---|---|---|
| 登录 | 账号密码，JWT 会话 | 全部 |
| 数据总览 | 按学年：年级/班级/学生数、成绩导入情况、综测完成度 | 全部 |
| 成绩导入 | 上传 → 预览/异常 → 弹窗新建缺失项 → 确认入库 | 全部（辅导员限所辖年级） |
| 综测录入 | 选学年+班级，表格化填写五项，支持 2.xlsx 导入 | 全部（限所辖年级） |
| 学生管理 | 查询/修正学籍（降级生、班级变动） | 全部（限所辖年级） |
| 导出中心 | 选学年+年级 → 下载 xlsx | 全部（限所辖年级） |
| 基础数据 | 学年/年级/班级管理，辅导员绑定查看 | 全部（均可新建） |
| 账号管理 | 辅导员增删改、重置密码、绑定年级 | 管理员 |
| 综测方案 | 各学年五项配置、合成权重 | 管理员 |
| 等级换算表 | 等级→分数维护 | 管理员 |
| 操作日志 | 导入/导出/删除等审计记录 | 管理员 |

## 7. 技术方案

- **后端**：Python 3.10+ / FastAPI + SQLAlchemy + SQLite（单文件库 `data/zongce.db`，面板直接备份；量大可平滑切换 MySQL）；
- **Excel**：xlrd 读 .xls（已验证可读样本 1.xls）、openpyxl 读写 .xlsx；纯 Python 实现透视，无 pandas 依赖；
- **前端（v1.2 改造）**：React 18 + **Ant Design 6**（antd 6.6.2）+ **Ant Design Pro 组件体系** `@ant-design/pro-components`（ProLayout / PageContainer / ProTable / ProForm 等），Vite 构建，构建产物由 FastAPI 静态托管（宝塔上只跑一个 Python 进程）；
  - 选型说明：`@ant-design/pro-components` 2.x 稳定版仅兼容 antd ≤5，antd 6 需搭配官方 3.x 系列（peer `antd ^6`）；故锁定 `pro-components@3.1.14-7` + `antd@6.6.2` + `@ant-design/icons@6` 组合升级；
  - antd 6 关键约定（写代码须遵守）：React ≥ 18；CSS Variables 默认开启；`Button` 的 `type` 样式职责拆分为 `color` + `variant`；`Modal`/`Tabs` 用 `destroyOnHidden`；`Select` 用 `variant`、`popupMatchSelectWidth`、`classNames.popup.root`；`Tabs` 用 `tabPlacement`；`Statistic` 用 `styles.content`；`Table.size` 仅 `large/medium/small`；全局用 `App` 组件包裹并经 `App.useApp()` 取 `message/modal/notification`；
- **认证**：JWT（PyJWT）；密码 PBKDF2-SHA256 加盐（标准库实现，无额外依赖）；
- **部署**：宝塔「Python项目管理器」+ Nginx 反代，见《部署文档-宝塔.md》。

## 8. 验收标准

1. 上传样本 `1.xls` → 导出 `24级（成绩）/24级（绩点）` 与 `output.xlsx` 对应 sheet 的学生、课程、成绩、加权平均值一致（可对照验证）；
2. 导入样本 `2.xlsx` → 五项得分、封顶、五项合计与源文件一致；合成成绩与年级内排名计算正确；
3. 导出的工作簿结构与 output.xlsx 类似（双层表头 + 稀疏矩阵 + 统计列 + 排名列）。

## 9. 边界与已知约定

- 1.xls 中「合格」按两级制=80 分、「及格」按五级制=65 分；
- 班级名统一入库（如「建筑类2401」），导入时自动剥离「班」后缀做匹配；
- 同名课程多课程代码（如大学英语㈡ 多代码）按不同课程列展开，与 output.xlsx 行为一致；
- 降级生：学生可手动调整所属班级，成绩仍按学号归档。

## 10. v1.1 功能增补（缺口补齐）

1. **补考/重修取分规则**（可配置，默认"取最新学期成绩"，可选"取最高分"）：同一学生同一课程代码在多个学期出现时按规则取一条参与矩阵与加权统计；
2. **导入批次管理**：每次导入生成批次并快照被覆盖旧值；支持整批撤销（恢复旧值/删除新插入记录）；管理员可按学年+年级清空成绩或综测记录；
3. **综测汇总在线浏览页**：选学年+年级即看按综测排名排序的合成成绩大表（五项/综素/综合测评成绩/双排名），无需导出；
4. **综测简表导出**：不含课程矩阵的单 sheet 简表（评奖/存档用），导出中心可选完整工作簿或简表；
5. **学生名单独立导入**：支持「学号/姓名/班级」名单表，先建档案后导成绩；含样例模板下载、缺失班级映射与冲突裁决；
6. **综测录入提效**：批量填充基础分模板（自动换算模板得分）、复制上一学年（仅填充未录入学生）、行内得分编辑模式；
7. **明细与得分软校验**：解析明细中「±数字」自动求和与手填得分比对，不一致标"明细不符"提醒（只统计带符号数字，避免"六级成绩587分+5"误判）；
8. **综测方案按年级差异化**：默认方案（学年级）+ 年级专属方案两级，计算时年级专属优先；
9. **账号安全**：修改/重置密码后旧凭证立即失效（token 版本号）；新建辅导员账号与被重置账号首次登录强制改密；连续 5 次登录失败锁定 10 分钟；
10. **导入冲突人工裁决**：姓名/班级与系统不一致时，可在确认弹窗中逐条选择"采用文件值"或"保留系统值"；
11. **特殊成绩保留**：缓考/缺考/未知等级的记录保留入库（不计入加权统计），在学生成绩明细中可见，不再整行丢弃；
12. **操作日志检索与分页**：按操作人/类型/日期范围筛选，服务端分页；
13. **异常清单导出**：成绩导入预览的异常清单可一键导出 CSV；
14. **学院维度**：学院作为班级的分组标签，支持学院管理、班级按学院筛选（权限仍按年级）；
15. **工程加固**：SQLite 启用 WAL 与 busy_timeout 缓解并发写锁；新增后端单元测试（10 项）覆盖换算/解析/校验/排名/补考规则。

## 11. v1.1.1 深度审查修复（2026-09-01）

1. 学生删除（管理员，级联删除其成绩与综测并记录日志）；
2. 账号启用/禁用开关（编辑弹窗）；
3. 学年改名与格式校验（YYYY-YYYY 且结束年=开始年+1）、班级改名与学院调整；
4. 综测汇总页班级筛选 + 学号/姓名搜索；
5. 总览页「未录名单」下钻（查看某年级未录入综测的学生）；
6. 汇总页/简表导出/学生报告区分「未录入」（显示 — 或"未录入"）与 0 分；
7. 综测方案覆盖标识：显示已有专属方案的年级，一键清除恢复跟随默认；
8. 导入批次历史清理（清理 90 天前快照，不影响业务数据）；
9. 复制上一学年按目标方案映射项目名，无法映射的项目跳过并计数；
10. 全站错误提示增强：FastAPI 422 校验错误可读化；admin 使用默认密码时强制改密。

## 12. v1.2 前端改造（2026-09-02）

1. 前端从「Ant Design 5」升级为「Ant Design 6 + Ant Design Pro 组件体系」：管理壳采用 `ProLayout`（侧边菜单/页头/水印/登录态），列表页统一 `ProTable`（查询表单、服务端分页），表单弹窗统一 `ProForm`/`ModalForm`/`StepsForm`，页面容器统一 `PageContainer`；
2. 锁定版本：`antd@6.6.2`、`@ant-design/pro-components@3.1.14-7`（官方声明 peer `antd ^6`）、`@ant-design/icons@6`；antd 6 的 API 约定见 §7，开发期用 `@ant-design/cli`（`antd info/demo/lint`）核对 API 与弃用项；
3. 权限路由：管理员/辅导员菜单按角色与所辖年级过滤；路由守卫基于 JWT 与 `token_version`；登录/改密页使用 ProForm，全站通知经 `App.useApp()`；
4. 布局与页面清单不变（§6），页面实现全部落在 Ant Design Pro 组件体系上。

## 13. v1.3 实现级设计增补（2026-09-02，落地版）

> 本章把前述需求固化为可直接编码的实现规格：数据库表结构、API 清单、项目结构与导出工作簿逐列规格。版本核实：`antd@6.6.2`、`@ant-design/pro-components@3.1.14-7`、`@ant-design/icons@6.3.4` 均已确认存在于 npm，版本锁定可执行。

### 13.1 数据库表结构（SQLite，SQLAlchemy 模型）

```
users                id, username UNIQUE, password_hash, salt, role(admin|counselor),
                     real_name, enabled BOOL, must_change_password BOOL,
                     token_version INT default 1, failed_attempts INT, locked_until DATETIME
user_grades          user_id FK, grade_id FK            -- 辅导员↔年级 多对多
colleges             id, name UNIQUE
academic_years       id, name UNIQUE('YYYY-YYYY')
grades               id, name UNIQUE('XX级'), enrollment_year INT
classes              id, name UNIQUE, grade_id FK, college_id FK NULL,
                     major VARCHAR NULL  -- 显式专业；空则排名分组按班名自动提取
students             id, student_no UNIQUE, name, class_id FK          -- 班级可改（降级生）
score_records        id, student_id FK, academic_year_id FK, semester(秋季|春季),
                     course_code, course_name, teacher, credit REAL NULL,
                     score_raw, score_num REAL NULL, gpa REAL NULL, is_elective,
                     course_category, retake_type, batch_id FK NULL,
                     UNIQUE(student_id, academic_year_id, semester, course_code)
eval_records         id, student_id FK, academic_year_id FK, item_name, detail_text,
                     score REAL, batch_id FK NULL,
                     UNIQUE(student_id, academic_year_id, item_name)
eval_schemes         id, academic_year_id FK NULL(=全局默认方案), grade_id FK NULL(=年级专属),
                     weight_academic REAL default .8, weight_eval REAL default .2,
                     items JSON  -- [{name, max_score, base_template}]
grade_conversions    id, level_text UNIQUE, score REAL, level_group(百分制|两级制|五级制)
import_batches       id, kind(score|eval|student), filename, academic_year_id FK NULL,
                     operator_id FK, created_at, snapshot JSON, stats JSON, reverted BOOL
operation_logs       id, operator_id FK NULL, action, detail, created_at
```

关键约定：
- `score_records.score_num` 为换算后百分制；等级文本成绩按 `grade_conversions` 换算，`score_raw` 保留原文；缓考/缺考/未匹配等特殊成绩 `score_num` 置 NULL 保留入库（不计入加权统计）；
- 课程无独立表：导出时按 `course_code` 聚合 `score_records`，名称/学分/教师取出现次数最多值（计数平票取字典序首个，保证稳定）；
- 补考/重修取分规则存于 `eval_schemes.retake_rule`（latest|highest，默认 latest），作用于「同学生同课程代码跨学期多条」的收敛；
- `import_batches.snapshot` 记录被覆盖旧记录的完整字段与被删记录主键，支撑整批撤销；
- 密码 PBKDF2-SHA256 100k 迭代 + 16 字节盐；JWT 载荷含 `token_version`，改密/重置后自增使旧凭证失效。

### 13.2 API 清单（前缀 `/api`，除登录外均需 `Authorization: Bearer`）

| 分组 | 端点 | 方法 | 说明 |
|---|---|---|---|
| 认证 | /auth/login | POST | 登录，5 次失败锁 10 分钟 |
| 认证 | /auth/me · /auth/password | GET · PUT | 当前用户 · 改密（token_version+1） |
| 基础数据 | /colleges · /academic-years · /grades · /classes | GET/POST/PUT/DELETE | 学院、学年、年级、班级 CRUD |
| 基础数据 | /students · /students/import · /students/template · /students/{id} | GET/POST/… | 学生分页查询、名单导入、模板下载、修正/删除 |
| 成绩导入 | /scores/import/preview | POST(file) | 解析→学年/学期识别、待建项、异常清单 |
| 成绩导入 | /scores/import/confirm | POST | 确认入库（建批次、覆盖旧记录、快照） |
| 成绩导入 | /scores | GET | 学生成绩明细查询（按学年/班级/学生） |
| 成绩导入 | /scores/exceptions/export | POST | 异常清单导出 CSV |
| 综测 | /evals/roster · /evals · /evals/batch | GET/PUT/POST | 名册+已有记录、单条保存、批量保存 |
| 综测 | /evals/import/preview · /evals/import/confirm | POST | 2.xlsx 解析（双层表头/合并单元格/班级模糊匹配）与入库 |
| 综测 | /evals/copy-prev · /evals/fill-base | POST | 复制上一学年、批量填充基础分模板 |
| 汇总 | /summary | GET | 合成成绩大表（综测排名排序，班级筛选/搜索） |
| 导出 | /export/workbook · /export/brief | POST→文件流 | 完整工作簿（多年级多 sheet）/ 综测简表 |
| 导出 | /export/student-report | GET | 单学生报告；省略学年参数时返回历年总览（各学年学业/综测/双排名汇总） |
| 方案 | /schemes · /schemes/grade | GET/POST/DELETE | 默认方案、年级专属方案（覆盖标识/清除） |
| 换算表 | /conversions | GET/POST/DELETE | 等级换算表维护 |
| 账号 | /users | GET/POST/PUT/DELETE | 辅导员管理（禁用/重置/绑定年级） |
| 日志 | /logs | GET | 分页+操作人/类型/日期筛选 |
| 批次 | /batches · /batches/{id}/revert · /batches/cleanup | GET/POST | 批次历史、整批撤销、90 天快照清理 |
| 数据 | /data/clear | POST | 按学年+年级清空成绩或综测记录（管理员，§10.2） |
| 总览 | /overview | GET | 学年维度统计与综测完成度、未录名单下钻 |

权限：辅导员可访问端点一律按所辖年级过滤（无绑定年级则只见空集）；管理员全量。错误统一 `{detail: {message, errors?}}`，422 校验错误后端可读化。

### 13.3 项目结构

```
综测计算工作台/
├─ backend/
│  ├─ app/
│  │  ├─ main.py            # FastAPI 装配、静态托管 frontend/dist、CORS
│  │  ├─ database.py        # engine(SQLite WAL/busy_timeout)、SessionLocal、Base
│  │  ├─ models.py          # §13.1 全部表
│  │  ├─ schemas.py         # Pydantic 请求/响应模型
│  │  ├─ auth.py            # JWT、PBKDF2、依赖注入 current_user/require_admin
│  │  ├─ services/
│  │  │  ├─ score_import.py # 1.xls/.xlsx 长表解析、类型归一、学年/学期归类
│  │  │  ├─ eval_import.py  # 2.xlsx 双层表头/合并单元格解析、班级模糊匹配
│  │  │  ├─ convert.py      # 等级换算、明细「±数字」软校验求和
│  │  │  ├─ calc.py         # 取分规则、加权平均、综测合成、排名
│  │  │  └─ export.py       # output.xlsx 风格工作簿/简表生成
│  │  └─ routers/           # auth/base_data/scores/evals/summary/export/
│  │                        # schemes/users/logs/batches/overview
│  └─ tests/test_rules.py   # §10.15 单元测试 ≥10 项
├─ frontend/                # Vite + React 18 + antd 6 + pro-components（本目录现为 antd 参考仓库，
│                           # 实际应用建于 frontend/app/，构建产物 dist 由后端静态托管）
├─ data/zongce.db           # SQLite 单文件
└─ 产品设计文档.md · 部署文档-宝塔.md
```

### 13.4 导出工作簿逐列规格

成绩 sheet（每年级一个，名 `XX级（成绩）`）：
- 第 1–3 行表头：`A/B` 列留白合并；`C` 列自上而下为「课程代码/课程名称/学分」元标签（对齐手工样本），课程列自 **D 列** 起，依次写各课程代码/名称/学分（课程列序：秋季在前、春季在后，同学期按代码升序）；
- 表头末行（第 4 行）为标签行：`A=班级|B=学号|C=姓名`；数据行按班级分组（班内学号升序），未修课程留空，成绩矩阵填换算后百分制；
- 尾部列（紧随最后一个课程列）：`加权总分|总学分|加权平均值|德育|思想品德|社会工作|科研及科技创新|文体活动|集体建设|综合素质测评|综合测评成绩|智育排名|综测排名|确认签字`；
- `加权平均值` 单元格写公式 `=加权总分单元格/总学分单元格`；`德育`、`确认签字` 留空；未录综测者五项与综合测评显示 0 并标记填充色；无成绩学生加权三列留空且不参与智育排名。

绩点 sheet（`XX级（绩点）`）：表头 4 行（课程代码/课程名称/授课教师/学分），矩阵填绩点值，尾部列同上（智育排名按平均绩点？否——仍按学业加权平均，与成绩 sheet 一致，便于核对）。

简表 sheet（`XX级综测简表`）：`班级|学号|姓名|学业加权平均|五项小计×5|综合素质测评|综合测评成绩|智育排名|综测排名|确认签字`，未录显示「未录入」。

### 13.5 计算引擎流程（calc.py）

1. 取学生该学年全部成绩记录 → 按补考规则收敛为每课程代码一条（latest：学期字典序最大；highest：score_num 最大；两者对 `score_num IS NULL` 记录取最新）；
2. `加权总分=Σ(score_num×credit)`、`总学分=Σ(credit)`（仅 score_num 与 credit 均非空），`加权平均值=加权总分/总学分`；
3. 五项小计 `min(得分, 满分)`（无记录=0 分，标记未录入）；`综合素质测评=Σ小计`；
4. `综合测评成绩 = 加权平均值×weight_academic + 综合素质测评×weight_eval`（方案：年级专属覆盖默认）；
5. 排名：专业组（同年级、班级名去尾部数字，见 §5.6）内分别按加权平均、综合测评成绩降序，同分同名次（1,1,3 式，competition ranking）；无加权平均值者不参与智育排名。

## 14. v1.3.1 完整性审查修复（2026-09-02）

> 全量审查（需求逐条 → 端点 → 前端 UI 链路）后修复 11 项，均含自动化回归（`tests/e2e_check.py` §9–§15）。

1. **数据清理落地（§10.2 缺口）**：新增 `POST /api/data/clear`（管理员），按学年+年级清空成绩或综测记录并记日志；「日志与批次」页新增「数据清理」Tab（危险操作二次确认）；
2. **422 可读化补全（§11.10）**：`main.py` 注册 `RequestValidationError` 处理器，pydantic 校验错误输出可读中文 `{detail:{message,errors}}`；前端 `readError` 兼容数组型 detail 兜底；
3. **名单导入越权修复**：`/base/students/import` 对已存在班级校验辅导员年级权限（原先可借导入修改任意年级学生档案）；响应新增 `conflicts` 字段，姓名/班级与系统不一致在前端可见；
4. **成绩导入自动绑定年级（§4.1.3）**：`confirm_score_import` 新建年级时若操作者为辅导员自动绑定（原先仅手动新建/名单导入路径生效，辅导员导完成绩反而无权访问新年级）；
5. **批次撤销数据丢失修复（严重）**：`ImportBatch.snapshot` 为普通 JSON 列，flush 后的就地 `append` 不被变更检测跟踪，**快照从未入库**——撤销时恢复 0 条、误删被覆盖记录。改用 `MutableList.as_mutable(JSON)`；撤销流程先 flush 恢复值再按批次删除；快照记录增加 `batch_id` 溯源字段；
6. **/evals/batch 单事务**：抽取 `_save_eval_records`（不提交），全班保存任一学生失败整体回滚；
7. **/api 未匹配路径返回 404 JSON**：SPA 兜底路由之前增加 `/api/{path}` 拦截，不再把 API 404 渲染成 index.html；
8. **综测汇总页搜索修复**：`useEffect` 依赖补 `keyword`（原先回车搜索不触发刷新）；
9. **学生页班级筛选修复**：ProTable「班级」列由纯文本改为班级下拉（`valueType:select`），接通后端 `class_id` 参数（原先填写不生效）；
10. **基础数据页补全**：学年 Tab 新增「改名」入口（管理员，接 `PUT /academic-years`，§11.3）；班级「编辑」仅管理员可见；班级 Tab 新增按学院筛选（§10.14）；
11. **成绩样例模板修复**：`HEADER_MAP` 补「学生标签」映射（新教务格式班级列）。原先照模板填写导入时班级全为空、记录全部被跳过。另：年级专属方案编辑器预填该学年实际默认方案、选中已有专属方案时载入现有配置。

## 15. v1.3.2 排名口径调整（2026-09-02）

**智育排名与综测排名由「年级统排」改为「专业内排名」**：

1. 专业组定义：同年级内、班级名去掉尾部「班号数字+可选班字」后相同的班级（建筑类2401/建筑类2402班 → 建筑类；不含班号的班名保持原样）。年级仍隔离：不同年级的同名专业不一起排；
2. 排名口径与展示筛选无关：综测汇总页的班级筛选、导出中心的单班级导出、单个学生报告，得到的排名均为专业组全体口径（此前按传入子集计算，学生报告的排名恒为 1，属缺陷）；
3. 同分同名次（1,1,3 式）、无加权平均值不参与智育排名等规则不变；
4. 涉及代码：`calc.py` 新增 `major_group()`，`compute_grade()` 先对专业组全体计算再返回展示子集；导出工作簿/简表尾部排名列随之生效；
5. 回归：`test_rules.py::test_major_group`；e2e §15 构造同年级「计科2401」专业高分学生，断言建筑类 50 人排名不受影响、计科学生在本专业组内双第 1、末名学生报告排名与汇总页一致。

## 16. v1.3.3 功能增补（2026-09-02）

1. **班级「专业」字段（可选）**：`classes.major` 列（旧库启动时自动迁移补列）；新建/编辑班级可填写，排名分组**显式专业优先**，留空则按班级名自动提取（v1.3.2 规则）；基础数据页班级表显示有效分组（自动提取的带「自动」标识）；
2. **综测导入改为按年级**：导入弹窗选择「目标学年 + 目标年级」，系统**仅按学号匹配所选年级内的学生**，不再要求逐班映射（文件班级列仅作参考展示）；未匹配（年级外学号/不存在）不入库并在预览中标出；辅导员权限按所选年级鉴权（403）；`/evals/import/preview|confirm` 增加 `grade_id` 参数、confirm 不再接收 `class_map`；
3. 回归：e2e §5（年级导入流程 + 匹配计数）、§12（综测导入年级鉴权）、§15b（显式专业合并排名 → 清除后恢复自动分组）。

## 17. v1.3.4 学生管理页数据维度增强（2026-09-02）

1. **学生报告抽屉组件化**：`StudentReportDrawer` 前后端共用（综测汇总页/学生管理页），综测汇总页行为不变（固定学年）；
2. **学生管理页「报告」按钮**：每行操作列新增「报告」，弹窗展示——历年总览表（各学年：成绩记录数 / 学业加权平均 / 平均绩点 / 综测 / 综合测评成绩 / 智育·综测双排名（专业组内）/ 特殊成绩），点击学年行或下拉切换查看该学年明细（统计 + 综测明细 + 课程成绩）；默认定位到最近一个有数据的学年；未录入与 0 分的展示区分沿用 §11.6；
3. **后端**：`/export/student-report` 的 `academic_year_id` 改为可选——省略时返回 `summaries`（按学年倒序的历年汇总，含 `has_data` 标记）；排名口径与汇总页一致（专业组内）；
4. 回归：e2e §15c（历年总览 3 学年、成绩维度与综测维度分别断言、排名与汇总页一致）。

## 18. v1.3.5 登录页改版（2026-09-02）

按提供的 auth-pages-template 风格重做登录页（纯 CSS 实现，未引入 Tailwind）：左侧深绿品牌面板（放射光晕、同心圆环与金色点缀、层叠山丘、三条平台特性卡片），右侧浅色背景 + 白色圆角登录卡片（带图标输入框、密码可见切换、字段级校验红框、翡翠绿主按钮与加载态）。登录/首登强制改密/锁定提示等逻辑不变；窄屏（<1024px）自动隐藏左栏并显示顶部品牌。实现：`Login.jsx` + `Login.css`，不再使用 `LoginFormPage`（§12.3 相应口径以本节为准）。

## 19. v1.3.6 学生管理页维度增强（2026-09-02）

1. **学年维度指标列**：工具栏新增学年选择（默认最近学年），列表新增「学业加权平均 / GPA / 综合素质测评 / 综合测评成绩」四列（综测未录显示「未录入」，与 0 分区分）；
2. **全列表头点击排序**：学号/姓名/班级/年级与四个指标列均支持服务端排序（指标为计算值，客户端排序只能作用于当前页故不采用）；指标列排序时无值学生恒排末尾；
3. **后端**：`/base/students` 增加 `academic_year_id / sort / order` 参数；`calc.py` 抽取纯计算函数 `_aggregate_metrics` 并新增 `compute_metrics_bulk`（每年级 2 次批量查询），`compute_grade` 复用后汇总/导出/报告的查询量由每生 2 次降为每年级 2 次；
4. 回归：e2e §15d（加权平均降序、无成绩者殿后、综测字段、学号升序）。

## 20. v1.3.7 专业列与专业筛选（2026-09-02）

1. **学生管理页**：新增「专业」列（显示班级有效专业：显式专业优先，自动提取的标注「自动」），查询表单新增专业下拉（选项来自所辖班级的有效专业去重）；四个指标列（学业加权平均 / GPA / 综合素质测评 / 综合测评成绩）不再生成查询输入框（`hideInSearch`）；
2. **综测汇总页**：筛选区新增「专业」下拉（随年级联动，切换年级自动重置）；导出简表/工作簿时专业筛选自动换算为对应 class_ids 生效；
3. **后端**：`/base/students` 与 `/summary` 增加 `major` 参数，按班级有效专业（显式专业优先、否则 `major_group` 班名提取）匹配；学生列表项新增 `major / major_effective` 字段，专业列支持服务端排序；
4. 回归：e2e §15e（学生列表与汇总页的专业筛选计数与字段值）。

## 21. v1.3.8 综测导入交互改版（2026-09-02）

综测导入从「综测录入页内的抽屉」改为独立菜单页（菜单位次紧随「成绩导入」），交互与成绩导入一致：页面顶部选择目标学年与年级 → 拖拽/点击上传 → 同页展示「导入预览」卡片（文件、范围、学生数、匹配成功、项目与满分、明细不符、未匹配清单），「确认入库」位于预览卡片右上角，成功后清空重置。综测录入页的「导入 2.xlsx」按钮改为跳转该页，旧抽屉删除。修复：ImportEvalModal 缺少 `modal` 解构导致的「modal is not defined」崩溃（导入本身在崩溃前已完成入库）。实现：`EvalImport.jsx`（新增）+ `App.jsx` 菜单/路由。

## 22. v1.3.9 样例表格与界面文案清理（2026-09-02）

1. **界面移除「1.xls」「2.xlsx」样例文件名字样**：成绩导入、综测导入页面描述与综测录入页按钮文案改为通用表述（"样例表格""导入综测明细"）；
2. **样例表格下载**：成绩导入页新增「下载样例表格」按钮（`GET /scores/template`，此前端点已存在但无前端入口）；综测导入页新增同款按钮与后端 `GET /evals/template`——生成双层表头样例（项目大类合并单元格跨「加减分项/得分」两列、表头携带满分、含总分列与两条样例数据），可被导入解析器直接读取（e2e §15f 验证五项满分与明细软校验）；
3. 版本号 1.3.9。
